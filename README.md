# Autoencoder-for-Super-Resolution
This work addresses an image super-resolution task on the EuroSAT dataset. Three different neural network architectures were evaluated: one operating on 32×32 low-resolution inputs and two on 16×16 inputs. All models aim to reconstruct high-resolution images at 64×64 resolution.
